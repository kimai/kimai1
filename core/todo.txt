++TODO++



# Opera: Filter verrutschen nach oben




! Der Step-by-Step installer erstellt keine datenbank 
  (egal ob der user die rechte hat oder nicht ...)
--> FEATURE IST AUS IN 0.8.0
  
...


@ errorreporting aus - sonst gibt’s morddrohungen ... @done

@ Nach der Installation ist der Gruppe Admin nicht Gruppenleiter "admin" zugeordnet

@ Nach update von 0.7.11 ist der admin user weg! (nur mit einem user getestet - ich weiß nicht ob er andere auch droppt) @done
  mit dem backup kann ich danach problemlos wieder rein (braucht in der conf nur der prefix geändert werden)


@ Spachabhängige Testeinträge in KND/PCT/EVT @done
@ zef_cleared @done

[@oleg]: das engl. readme ist fuerchterlich ;) verzier' das doch mal mit deiner
poesie ;)


---------------------------------------



@ Wir müssen die Installation vereinfachen. Sowas wie ein Ajax-driven 1-2-3 @done

---

Kompatibilität

@ die neusten Versionen von Safari 3, FF 3, Opera 9.5 und IE 7 @done
@ [ich würge sagen das ist erledigt. sogar IE7 tuts jetzt....] WIRKLICH??? Habe von Problemen gehört ....

---


CVS_ext? Brauchen wir eine Exportfunktion schon in der 0.8? Wenn ja - kann
oder sollte die in die stats_ext eingebaut werden?

---

Stats_ext

# Neues Frontend von TH? 
# TSS nutzen statt eigene Auswahl-Pulldowns - die Auswahl kommt dann per HOOK rein. 

# PDF? # XML? # CVS?

  Antwort: CVS-Imports und Exports wären tatsächlich sehr interessant, es gibt da
  auch bereits PHP-Klassen dafür.

---

TS_ext

@ Buttons zum Anlegen von knd/pct/evt erscheinen nur noch im Admin-Modus. Die
@ unteren Drei Tabellen sind hier die gleichen wie im AP aber sie merken welche
@ Rechte der Nutzer hat. @done 

---

0.8.1 -> Tastenkürzel im gesamten UI?

0.8.1 -> Funktionen: Timespace setzen für user, temporären timespace für admin oder GL
0.8.1 -> erzeugen, ... (schreibe ich auch später nochwas zu)





------------------------------------------

[TH]:

@ message seite reparieren - neues CSS @done

@ nur *eine* on_resize funktion die jeweils on tabchange initialisiert
  wird - damit die nicht alle gleichzeitig die ganze zeit im hinter-
  grund laufen @done
  
0.8.X ->(# Symbole/Logos im BUZZ ermöglichen ....)

@ Debug-Ext: Laenge des Logs beschraenken, da sonst Browser-Crash bei zu langem Log  @done

@ get_timespace(): Bei erstmaligem Aufruf (beim login) ist der index 'usr' nicht definiert (db_layer.php Zeile 3098)  @done

------------------------------------------

[OB]: (bitte abhaken wenn erledigt ;)

? function im DB layer für entry-delete von zef
  (siehe TS processor case: quickdelete)



@ db-layer: pct_get_data sollte noch den Kunden-Namen mit ausgeben @wird alternativ gelöst


!!---

# Das AP darf als Extension nur geladen werden, wenn ein Admin oder GL
  angemeldet ist und muss auch zwischen diesen beiden noch unterscheiden können.
  Die Ext läd für den GL weniger Funktionen als für den Admin.
  
  L_____  Antwort: Ich stelle mir das eher so vor, dass GLs eine eigene ext kriegen. Wir
          machen erst die AP fertig, machen dann eine Kopie davon, und specken sie so
          weit ab, dass die GL-AP daraus wird. Wir sollten erstmal das AP machen, um
          redundante Arbeit zu vermeiden.
  
          Ich bin deswegen gegen Admin-AP und GL-AP in einer Ext, weil das den Code
          ziemlich unübersichtlich machen würde (ständig "Abzweigungen" für jeweils
          Admin oder GL).
          
          L______  OK leuchtet ein. Du machst das schon ;)
                  
  
# Der Updater sollte selbst ein Backup machen

  L_____  Antwort: Wie wäre es, wenn er dem Update-Vorgang einfach alle vorhandenen
          Tabellen nimmt und mit "_bak" oder so am Ende kopiert, und dann erst den
          Updatevorgang durchführt?
          
          L_____    Das wäre für das update total ausreichend.
          
          			L_____ habe das mal mit dem Praefix backup_[serverpraefix]tabelle
          				   realisiert - vll uebersichtlicher so, wenn alle Tabellen mit "backup"
          				   anfangen
          				   @todiscuss sollen die Backup-Tabellen nach erfolgreichem Update
          				   geloescht werden?
          			
                    Die richtige Backup-Funktion im Backend müsste natürlich eine
                    Datei exportieren können
  
  

  
@ Funktion: Überprüfung der zef-Tabelle auf Inkonsistenzen (z.b. >1 Eintrag ohne Endzeit => alle bis
  auf den mit der neuesten Starzeit stoppen - oder löschen?) return: true wenn alles okay, false wenn
  es einen Fehler gab @done - noch nicht implementiert!
  
  	L__________ erste Version in validate_zef() im DB Layer
  	
    
? get_arr_xxx_with_time neu schreiben @todiscuss

	L__________ die urspr. Version war soweit OK, d.h. wurde lediglich auf PDO umgestellt
	=> laeuft alles damit soweit?
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
